apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.11.1
  creationTimestamp: null
  labels:
    triggermesh.io/crd-install: "true"
  name: crdregistrations.scoby.triggermesh.io
spec:
  group: scoby.triggermesh.io
  names:
    kind: CRDRegistration
    listKind: CRDRegistrationList
    plural: crdregistrations
    shortNames:
    - crdreg
    singular: crdregistration
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.crd
      name: CRD
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: CRDRegistration uses existing CRDs to provide generic controllers
          for them.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: CRDRegistrationSpec defines the desired state of a CRD Registration
            properties:
              crd:
                description: Name of the CRD to be used.
                type: string
              hook:
                properties:
                  address:
                    properties:
                      ref:
                        description: Ref points to an addressable object.
                        properties:
                          apiVersion:
                            description: API version of the hook address.
                            type: string
                          kind:
                            description: 'Kind of the referent. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
                            type: string
                          name:
                            description: 'Name of the hook address. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names'
                            type: string
                          namespace:
                            description: 'Namespace of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
                              This is optional field, it gets defaulted to the object
                              holding it if left out.'
                            type: string
                        required:
                        - kind
                        - name
                        type: object
                      uri:
                        description: URI can be an absolute URL(non-empty scheme and
                          non-empty host) pointing to the target or a relative URI.
                          Relative URIs will be resolved using the base URI retrieved
                          from Ref.
                        type: string
                    type: object
                  capabilities:
                    description: Capabilities that a hook implements.
                    items:
                      description: Phase identifies the phases where hooks can intercept
                        the reconciliation process. Supported phases are `pre-reconcile`
                        and `finalize`
                      type: string
                    type: array
                  timeout:
                    description: Timeout for hook calls.
                    type: string
                  version:
                    type: string
                required:
                - address
                - version
                type: object
              workload:
                description: Workload is information on how to create the user workload.
                properties:
                  formFactor:
                    description: FormFactor indicates the kubernetes object that will
                      run instances of the component's workload.
                    properties:
                      deployment:
                        description: Deployment hosting the user workload.
                        properties:
                          replicas:
                            description: Replicas for the deployment.
                            type: integer
                          service:
                            description: Service to create pointing to the deployment.
                            properties:
                              port:
                                description: Port exposed at the service.
                                format: int32
                                type: integer
                              targetPort:
                                description: Port exposed at the target deployment.
                                format: int32
                                type: integer
                            required:
                            - port
                            - targetPort
                            type: object
                        required:
                        - replicas
                        type: object
                      knativeService:
                        description: KnativeService hosting the user workload.
                        properties:
                          maxScale:
                            description: MaxScale is the service maximum scaling replicas
                            type: integer
                          minScale:
                            description: MinScale is the service minimum scaling replicas
                            type: integer
                          visibility:
                            description: Visibility is the network visibility for
                              the service
                            type: string
                        type: object
                    type: object
                  fromImage:
                    description: FromImage contains the container image information.
                    properties:
                      repo:
                        description: Repo where the image can be downloaded
                        type: string
                    required:
                    - repo
                    type: object
                  parameterConfiguration:
                    description: ParameterConfiguration sets how object elements are
                      transformed into workload parameters.
                    properties:
                      add:
                        description: Add contains instructions to render elements
                          at the generated workload not derived from the user instance.
                        properties:
                          toEnv:
                            description: Render options for adding environment variables
                              unrelated to the user's object input.
                            items:
                              description: AddToEnvConfiguration are the customization
                                options for an environment variable added from scratch.
                              properties:
                                name:
                                  description: Name is the name of the environment
                                    variable to be created.
                                  type: string
                                value:
                                  description: Value is a literal value to be assigned
                                    to the parameter.
                                  type: string
                                valueFrom:
                                  description: Instructions to extract envrionment
                                    variables values from the object's registration.
                                  properties:
                                    configMap:
                                      description: Selects a key of a ConfigMap.
                                      properties:
                                        key:
                                          description: The key to select.
                                          type: string
                                        name:
                                          description: 'Name of the referent. More
                                            info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                            TODO: Add other useful fields. apiVersion,
                                            kind, uid?'
                                          type: string
                                        optional:
                                          description: Specify whether the ConfigMap
                                            or its key must be defined
                                          type: boolean
                                      required:
                                      - key
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    field:
                                      description: 'Selects a field of the pod: supports
                                        metadata.name, metadata.namespace, `metadata.labels[''<KEY>'']`,
                                        `metadata.annotations[''<KEY>'']`, spec.nodeName,
                                        spec.serviceAccountName, status.hostIP, status.podIP,
                                        status.podIPs.'
                                      properties:
                                        apiVersion:
                                          description: Version of the schema the FieldPath
                                            is written in terms of, defaults to "v1".
                                          type: string
                                        fieldPath:
                                          description: Path of the field to select
                                            in the specified API version.
                                          type: string
                                      required:
                                      - fieldPath
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    secret:
                                      description: Selects a key of a secret in the
                                        pod's namespace
                                      properties:
                                        key:
                                          description: The key of the secret to select
                                            from.  Must be a valid secret key.
                                          type: string
                                        name:
                                          description: 'Name of the referent. More
                                            info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                            TODO: Add other useful fields. apiVersion,
                                            kind, uid?'
                                          type: string
                                        optional:
                                          description: Specify whether the Secret
                                            or its key must be defined
                                          type: boolean
                                      required:
                                      - key
                                      type: object
                                      x-kubernetes-map-type: atomic
                                  type: object
                                valueFromControllerConfigMap:
                                  description: ValueFromControllerConfigMap adds an
                                    environment variable whose value is read from
                                    a ConfigMap at the controller's namespace.
                                  properties:
                                    key:
                                      description: The key to select.
                                      type: string
                                    name:
                                      description: 'Name of the referent. More info:
                                        https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                        TODO: Add other useful fields. apiVersion,
                                        kind, uid?'
                                      type: string
                                    optional:
                                      description: Specify whether the ConfigMap or
                                        its key must be defined
                                      type: boolean
                                  required:
                                  - key
                                  type: object
                                  x-kubernetes-map-type: atomic
                              type: object
                            type: array
                          toVolume:
                            description: Render options for mounting volumes unrelated
                              to the user's object input. Volume source must exists
                              at the user's namespace.
                            items:
                              description: Instructions to extract volume mount information
                                from the object's registration.
                              properties:
                                mountFrom:
                                  description: ValueFrom references an object to mount.
                                  properties:
                                    configMap:
                                      description: Selects a key of a ConfigMap.
                                      properties:
                                        key:
                                          description: The key to select.
                                          type: string
                                        name:
                                          description: 'Name of the referent. More
                                            info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                            TODO: Add other useful fields. apiVersion,
                                            kind, uid?'
                                          type: string
                                        optional:
                                          description: Specify whether the ConfigMap
                                            or its key must be defined
                                          type: boolean
                                      required:
                                      - key
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    secret:
                                      description: Selects a key of a secret in the
                                        pod's namespace
                                      properties:
                                        key:
                                          description: The key of the secret to select
                                            from.  Must be a valid secret key.
                                          type: string
                                        name:
                                          description: 'Name of the referent. More
                                            info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                            TODO: Add other useful fields. apiVersion,
                                            kind, uid?'
                                          type: string
                                        optional:
                                          description: Specify whether the Secret
                                            or its key must be defined
                                          type: boolean
                                      required:
                                      - key
                                      type: object
                                      x-kubernetes-map-type: atomic
                                  type: object
                                mountPath:
                                  description: Path where the file will be mounted.
                                  type: string
                                name:
                                  description: Name for the volume.
                                  type: string
                              type: object
                            type: array
                        type: object
                      fromSpec:
                        description: FromSpec contains instructions to generate workload
                          items from the instance's spec.
                        properties:
                          skip:
                            description: Skip sets whether the object should skip
                              rendering as a workload item.
                            items:
                              description: FromSpecToEnv is the customization option
                                to avoid an spec path from generating any rendering
                                output.
                              properties:
                                path:
                                  description: JSON simplified path for the parameter.
                                  type: string
                              required:
                              - path
                              type: object
                            type: array
                          toEnv:
                            description: Render options for generating environment
                              variables derived from the user's object input.
                            items:
                              description: FromSpecToEnv are the customization options
                                for an environment variable generated from an object
                                spec.
                              properties:
                                default:
                                  description: Default to be assigned to the parameter
                                    when a value is not provided by users.
                                  properties:
                                    configMap:
                                      description: Selects a key of a ConfigMap.
                                      properties:
                                        key:
                                          description: The key to select.
                                          type: string
                                        name:
                                          description: 'Name of the referent. More
                                            info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                            TODO: Add other useful fields. apiVersion,
                                            kind, uid?'
                                          type: string
                                        optional:
                                          description: Specify whether the ConfigMap
                                            or its key must be defined
                                          type: boolean
                                      required:
                                      - key
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    secret:
                                      description: Selects a key of a secret in the
                                        pod's namespace
                                      properties:
                                        key:
                                          description: The key of the secret to select
                                            from.  Must be a valid secret key.
                                          type: string
                                        name:
                                          description: 'Name of the referent. More
                                            info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                            TODO: Add other useful fields. apiVersion,
                                            kind, uid?'
                                          type: string
                                        optional:
                                          description: Specify whether the Secret
                                            or its key must be defined
                                          type: boolean
                                      required:
                                      - key
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    value:
                                      description: Value is a literal value to be
                                        assigned to the parameter.
                                      type: string
                                  type: object
                                name:
                                  description: Name is the name of the envr to be
                                    created.
                                  type: string
                                path:
                                  description: JSON simplified path for the parameter.
                                  type: string
                                valueFrom:
                                  description: ValueFrom uses a .
                                  properties:
                                    builtInFunc:
                                      description: BuiltInFunc configures the field
                                        to be rendered acording to the chosen built-in
                                        function.
                                      properties:
                                        args:
                                          description: The key to select.
                                          items:
                                            type: string
                                          type: array
                                        name:
                                          description: Function name
                                          type: string
                                      required:
                                      - name
                                      type: object
                                    configMap:
                                      description: Selects a key of a ConfigMap.
                                      properties:
                                        key:
                                          description: The key to select.
                                          type: string
                                        name:
                                          description: 'Name of the referent. More
                                            info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                            TODO: Add other useful fields. apiVersion,
                                            kind, uid?'
                                          type: string
                                        optional:
                                          description: Specify whether the ConfigMap
                                            or its key must be defined
                                          type: boolean
                                      required:
                                      - key
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    secret:
                                      description: Selects a key of a secret in the
                                        pod's namespace
                                      properties:
                                        key:
                                          description: The key of the secret to select
                                            from.  Must be a valid secret key.
                                          type: string
                                        name:
                                          description: 'Name of the referent. More
                                            info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                            TODO: Add other useful fields. apiVersion,
                                            kind, uid?'
                                          type: string
                                        optional:
                                          description: Specify whether the Secret
                                            or its key must be defined
                                          type: boolean
                                      required:
                                      - key
                                      type: object
                                      x-kubernetes-map-type: atomic
                                  type: object
                              required:
                              - path
                              type: object
                            type: array
                          toVolume:
                            description: Render options for mounting volumes derived
                              from the user's object input.
                            items:
                              description: FromSpecToVolume are the customization
                                options for a volume being mounted from configuration.
                              properties:
                                mountFrom:
                                  description: ValueFrom references an object to mount.
                                  properties:
                                    configMap:
                                      description: Selects a key of a ConfigMap.
                                      properties:
                                        key:
                                          description: The key to select.
                                          type: string
                                        name:
                                          description: 'Name of the referent. More
                                            info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                            TODO: Add other useful fields. apiVersion,
                                            kind, uid?'
                                          type: string
                                        optional:
                                          description: Specify whether the ConfigMap
                                            or its key must be defined
                                          type: boolean
                                      required:
                                      - key
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    secret:
                                      description: Selects a key of a secret in the
                                        pod's namespace
                                      properties:
                                        key:
                                          description: The key of the secret to select
                                            from.  Must be a valid secret key.
                                          type: string
                                        name:
                                          description: 'Name of the referent. More
                                            info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                            TODO: Add other useful fields. apiVersion,
                                            kind, uid?'
                                          type: string
                                        optional:
                                          description: Specify whether the Secret
                                            or its key must be defined
                                          type: boolean
                                      required:
                                      - key
                                      type: object
                                      x-kubernetes-map-type: atomic
                                  type: object
                                mountPath:
                                  description: Path where the file will be mounted.
                                  type: string
                                name:
                                  description: Name for the volume.
                                  type: string
                                path:
                                  description: JSON simplified path for the parameter.
                                  type: string
                              required:
                              - path
                              type: object
                            type: array
                        type: object
                      global:
                        description: Global defines the configuration to be applied
                          to all generated parameters.
                        properties:
                          defaultPrefix:
                            description: DefaultPrefix to be appeneded to keys by
                              all generated parameters. This configuration does not
                              affect parameter keys explicitly set by users.
                            type: string
                        type: object
                    type: object
                  statusConfiguration:
                    description: StatusConfiguration contains rules to populate a
                      controlled instance status.
                    properties:
                      add:
                        description: Add contains configurations for status elements
                          to be added.
                        items:
                          description: StatusAddElement is a customization option
                            that adds or fills an element at an object instance status
                            structure.
                          properties:
                            path:
                              description: JSON simplified path for the status element.
                              type: string
                            valueFrom:
                              description: ValueFrom an object element and use its
                                parameter to fill the status.
                              properties:
                                path:
                                  description: JSON simplified path for the referenced
                                    element.
                                  type: string
                              required:
                              - path
                              type: object
                          required:
                          - path
                          type: object
                        type: array
                      conditionsFromHook:
                        description: ConditionsFromHook contains conditions expected
                          to be informed from the Hook.
                        items:
                          description: ConditionsFromHook are extended conditions
                            that must be informed from the configured Hook.
                          properties:
                            type:
                              description: Type of the condition to be informed.
                              type: string
                          required:
                          - type
                          type: object
                        type: array
                    type: object
                required:
                - fromImage
                type: object
            required:
            - crd
            - workload
            type: object
          status:
            description: CRDRegistrationStatus defines the observed state of CRDRegistration
            properties:
              annotations:
                additionalProperties:
                  type: string
                description: Annotations is additional Status fields for the Resource
                  to save some additional State as well as convey more information
                  to the user. This is roughly akin to Annotations on any k8s resource,
                  just the reconciler conveying richer information outwards.
                type: object
              conditions:
                description: Conditions the latest available observations of a resource's
                  current state.
                items:
                  properties:
                    lastTransitionTime:
                      description: lastTransitionTime is the last time the condition
                        transitioned from one status to another. This should be when
                        the underlying condition changed.  If that is not known, then
                        using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: message is a human readable message indicating
                        details about the transition. This may be an empty string.
                      maxLength: 32768
                      type: string
                    reason:
                      description: reason contains a programmatic identifier indicating
                        the reason for the condition's last transition. Producers
                        of specific condition types may define expected values and
                        meanings for this field, and whether the values are considered
                        a guaranteed API. The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                        --- Many .condition.type values are consistent across resources
                        like Available, but because arbitrary conditions can be useful
                        (see .node.status.conditions), the ability to deconflict is
                        important. The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                description: ObservedGeneration is the 'Generation' of the Object
                  that was last processed by the controller.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
